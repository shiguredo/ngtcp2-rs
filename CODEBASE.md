# CODEBASE

- この指示がなくなるまでは変更履歴を `CHANGES.md` に残さないこと
- この指示がなくなるまでは PBT 対応はしないこと
- この指示がなくなるまでは Pull-Request やブランチを作らず develop にコミットしていくこと
  - 1 Issue 1 コミット 1 プッシュ

## 対応しない機能

- HTTP/3 には対応しないこと
  - ngtcp2 は QUIC のトランスポート層だけを提供する。HTTP/3 には nghttp3 のバインディングを新たに作る必要があり、このプロジェクトの対象外とする
- Windows には対応しないこと
  - パス情報の変換 (`libc::sockaddr_storage`) と ECN の送受信 (`cmsg`) に POSIX のソケット API を使っており、Windows で検証できる環境も無いため対象外とする

## 公開 API は必ず使用箇所とテストを用意すること

- どこからも呼ばれない関数・メソッドを追加してはいけない
- テスト・PBT で一度も使用されない公開 API を追加してはいけない
- 公開 API は必ずテストで動作を保証すること（テストされていない公開 API を公開しない）
- 既存の未使用・テスト未使用の公開 API を発見した場合は、テストを追加して動作を保証するか、削除して解消すること
- 理由: 使用箇所がなくテストもないコードは、リファクタリングや仕様変更で壊れても検出できず、ライブラリとしての信頼性を損なうため

## ngtcp2 の生 FFI 型を公開 API に出さないこと

- `shiguredo_ngtcp2_sys` の型 (`ngtcp2_transport_params` など) を公開シグネチャに露出させないこと
- 利用者に `shiguredo_ngtcp2_sys` への直接依存を強いるため
- 生型が必要な場合は Rust の型でラップし、`pub(crate)` で内部に閉じ込めること
